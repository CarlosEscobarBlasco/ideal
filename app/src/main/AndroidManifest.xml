<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carlos.ideal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".WelcomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.MainActivity"/>

        <activity android:name=".Activities.CreateIdeaActivity" />
        <activity android:name=".Activities.EditIdeaActivity" />
        <activity android:name=".Activities.IdeasListActivity" />
        <activity android:name=".Activities.MyIdeasActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".EditProfileActivity"></activity>
    </application>

</manifest>